import themes.sbss

#effect_loading {
    width: 36dp;
    height: 36dp;
    position: absolute;
    gravity: center;
    y: -40dp;
}

if $LOADING != "yes"
    #effect_loading {
        display: none;
    }

    /empty {
        script-when-loaded: hide;
    }
end
