import themes.sbss

#effect_loading: width=36dp, height=36dp, position=abs, gravity=center, y=-20dp

/empty/content: display=block, position=abs, gravity=center, y=-20dp
/empty/content/message: font-family=$SANS_KR_REGULAR, font-size=1.2, \
            text-align=center, text-color=$THEME_COLOR_WHITE_TEXT

if $LOADING != "yes"
    #effect_loading: display=none
else
    /empty/content: display=none
end
